public with sharing class ContactDiscountVIP implement IContactDiscount {

    public list<contact> calculateDiscount(){
        list<contact> contactList = [S];

        return contactList;
    };
}
