public with sharing class ContactDiscountVIP implement IContactDiscount {

    public list<contact> calculateDiscount(list<Contact> cList){
        list<contact> contactList = new list<contact>();

        return contactList;
    };
}
